import type{TinyRisk}from'./types';export const clampRisk=(n:number)=>Math.min(5,Math.max(1,Math.round(n)));export const riskScore=(probability:number,impact:number)=>clampRisk(probability)*clampRisk(impact);export const riskLevel=(score:number):'low'|'medium'|'high'|'critical'=>score>=20?'critical':score>=12?'high':score>=6?'medium':'low';export const sortRisks=(items:TinyRisk[])=>[...items].sort((a,b)=>b.score-a.score);